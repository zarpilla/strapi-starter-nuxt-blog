query Articles($slug: String){
  articles(where: { slug: $slug }) {
    id
    slug
    title
    content
    image {
      url
    }
    published_at
  }  
}

# query Articles($slug: String) {
#   article(slug: $slug) {
#     id
#     slug
#     title
#     content
#     image {
#       url
#     }
#     published_at
#   }
# }
